<!DOCTYPE html>
<html>
    <head>
        <title>Todolist</title>
    </head>

    <style>
        li{
            margin: .5rem .1rem;
            list-style: none;
            width: 350px;
        }
        span{
            background: rgb(148, 203, 255);
            padding: 5px;
            margin: .5rem 0;
            font-size: .7rem;
        }

        a{
            text-decoration: none;
            color: rgb(75, 73, 73);
        }
    </style>
    
    <body>
        <div class="todolist">
            <h2>ton top 5 de tes series </h2>
            <ul class="liste">
                <% maListe.forEach(function(todo, idx){
                    %>
                    <li><a href="<%= idx %>"> ✘ </a> <%= todo.serie %>  <span><a href="">🖊️</a></span></li>   
               <% }) %>              
            </ul>
            

            <form  method="POST">
                <label for="todo">Que dois-je acheter</label>
                <input type="text" name="titre" id="todo">
                <button type="submit">Valider</button>
            </form>
        </div>
    </body>
   
</html>